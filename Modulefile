name    'binford2k-structured_facts'
version '0.0.1'
author 'binford2k'
license 'Apache License, Version 2.0'
summary 'Parses JSON encoded facts into Puppet data structures'
description 'This function parses JSON encoded facts that begins with a magic marker string
  and sets corresponding global facts of the same name to the data structure that
  the original fact decodes to.

  Examples:

  * `encoded_ipaddresses` => `ipaddresses`
  * `encoded_mounts`      => `mounts`

  This function should be placed near the top of `site.pp`.'
project_page 'https://github.com/binford2k/structured_facts'
